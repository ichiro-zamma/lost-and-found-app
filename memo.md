・なぜenumを使った

「CHECK制約(値の範囲を制限する仕組み)をPrismaで宣言的に書く方法が、まだ実験的な機能でしか用意されていなかったため、Prisma標準の機能であるenumを使って同じ目的(値の範囲を制限すること)を実現した」

本来やりたかったこと
roleは「一般ユーザー(U)」か「施設管理者(A)」のどちらかしか入らないようにしたい。statusも「未マッチング(U)」「確認中(C)」「返却済み(R)」の3つしか入らないようにしたい、という制約を設けたかった。

最初の設計(CHAR(1) + CHECK制約)の課題
一般的なSQL設計ではCHECK (role IN ('U', 'A'))のように書けるが、Prismaのスキーマ言語ではCHECK制約を直接書く機能が正式にサポートされておらず(実験的機能はあるがバージョンによって不安定)、生成されたマイグレーションSQLを手動で書き換える必要があった。これは、今後スキーマを変更するたびに手動修正が消えたり壊れたりするリスクがあった。

enumを選んだ理由
Prismaのenumという標準機能を使えば、CHECK制約と同じ「決まった値しか入らない」という制約を、手動でのSQL編集なしに実現できる。加えて、TypeScript側でもRole.Uのように型として扱えるので、誤った値を書いた場合にコンパイル時点で気づけるという安全性のメリットもある。
トレードオフとして変わった点
DB上の実際の型は、当初想定していたCHAR(1)(固定長文字列)ではなく、PostgreSQLのENUM型に変わる。ただし、これは実装方法の違いであり、「値の意味」や「制約したい内容」自体は変わっていない。

値の範囲チェックをPrismaの標準機能(enum)で型安全に実装するため


・なぜポート番号が変わってデータが見れるようになったか
Prisma Studioが起動するたびに、ポート番号がランダムに変わっているためです。

何が起きているか
Prisma Studio is running at: http://localhost:51212
...
Prisma Studio is running at: http://localhost:49152

実行するたびに、51212だったり49152だったりと、毎回違うポート番号が表示されています。

なぜポート番号が変わるのか

.devcontainer/devcontainer.json の forwardPorts に、Prisma Studio 用の 5555 を追加していました

ところが、Prismaの新しいバージョン(今回使っている7.10.0)では、Studioが起動時にランダムな空きポートを選ぶという仕様になっているようです。これは手順書作成時点(v7.8.0)と、実際に使っているバージョン(v7.10.0)の間の、細かい仕様変更が原因と考えられます。

なぜ51212は繋がらず、49152は繋がったのか

これは、コンテナのdevcontainer.jsonで転送設定(forwardPorts)をしているポートが5555のみだからです。VS CodeのDev Containerは、「あらかじめ指定されたポート」か、「実行中に自動検出したポート」しか、ホスト(Mac)側に転送してくれません。

51212のとき → たまたま自動検出が間に合わなかった、またはタイミングの問題で転送されなかった
49152のとき → VS Codeが自動検出して、たまたま転送された

確実に繋がるようにする方法

毎回ポート番号を確認してアクセスするのは不便なので、Prisma Studioを固定のポート5555で起動するように指定するのが確実。

bash
pnpm exec prisma studio --port 5555

これでdevcontainer.jsonですでに転送設定してある5555が使われるので、毎回http://localhost:5555で確実にアクセスできる