・なぜenumを使った

「CHECK制約(値の範囲を制限する仕組み)をPrismaで宣言的に書く方法が、まだ実験的な機能でしか用意されていなかったため、Prisma標準の機能であるenumを使って同じ目的(値の範囲を制限すること)を実現した」

本来やりたかったこと
roleは「一般ユーザー(U)」か「施設管理者(A)」のどちらかしか入らないようにしたい。statusも「未マッチング(U)」「確認中(C)」「返却済み(R)」の3つしか入らないようにしたい、という制約を設けたかった。

最初の設計(CHAR(1) + CHECK制約)の課題
一般的なSQL設計ではCHECK (role IN ('U', 'A'))のように書けるが、Prismaのスキーマ言語ではCHECK制約を直接書く機能が正式にサポートされておらず(実験的機能はあるがバージョンによって不安定)、生成されたマイグレーションSQLを手動で書き換える必要があった。これは、今後スキーマを変更するたびに手動修正が消えたり壊れたりするリスクがあった。

enumを選んだ理由
Prismaのenumという標準機能を使えば、CHECK制約と同じ「決まった値しか入らない」という制約を、手動でのSQL編集なしに実現できる。加えて、TypeScript側でもRole.Uのように型として扱えるので、誤った値を書いた場合にコンパイル時点で気づけるという安全性のメリットもある。
トレードオフとして変わった点
DB上の実際の型は、当初想定していたCHAR(1)(固定長文字列)ではなく、PostgreSQLのENUM型に変わる。ただし、これは実装方法の違いであり、「値の意味」や「制約したい内容」自体は変わっていない。

値の範囲チェックをPrismaの標準機能(enum)で型安全に実装するため